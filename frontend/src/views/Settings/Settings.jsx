import { Box, Typography } from "@mui/material";

const Settings = () => (
  <Box sx={{ p: 3 }}>
      <Typography variant="h4" mb={2}>
        definiçoes
      </Typography>
    </Box>
);

export default Settings;
