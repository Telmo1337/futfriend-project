import BaseView from "../BaseView";
import { Typography } from "@mui/material";

const Notifications = () => (
  <BaseView title="Notificações">
    <Typography>Não tens novas notificações.</Typography>
  </BaseView>
);

export default Notifications;
